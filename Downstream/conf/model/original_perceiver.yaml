# ------------------------------------------------------------------------------
# Original Perceiver Model Configuration
# ------------------------------------------------------------------------------

# Encoder type (none for Perceiver)
encoder_type: null

# Decoder type
decoder_type: 'perceiver'

# Perceiver variant type
perceiver_type: 'original'  # Options: 'original', 'hierarchical'

# Architecture hyperparams
decoder_hidden_dim: 768
num_heads: 8
head_dim: 64
self_per_cross_attn: 1
n_blocks_decoder: 6

# Dropout
drop: 0.1

# Output dimensions
clip_seq_dim: 256  # Number of latent tokens
clip_emb_dim: 1664 # CLIP embedding dimension 